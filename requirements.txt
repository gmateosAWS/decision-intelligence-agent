# requirements.txt  ← CORREGIDO
#
# Cambios:
#   1. Versiones pinadas con ~= (compatible release) para garantizar reproducibilidad
#      sin bloquear parches de seguridad.
#   2. Añadido langchain-community (requerido para FAISS y otros integradores).
#   3. Añadido langchain-core (base de langchain, requerido explícitamente).
#   4. Versión de langgraph actualizada: la API StateGraph cambió significativamente
#      desde 0.0.40. Usando ~=0.2.0 que estabiliza la API actual.
#   5. Añadido pyyaml (requerido por la capa spec, Mejora 1).

# ── Core data & ML ─────────────────────────────────────────────────────────────
numpy~=1.26
pandas~=2.1
scikit-learn~=1.4
scipy~=1.12
networkx~=3.2

# ── LangChain ecosystem ────────────────────────────────────────────────────────
langchain-core~=0.3
langchain~=0.3
langchain-openai~=0.2
langchain-community~=0.3      # FAISS, document loaders y otros integradores

# ── LangGraph ─────────────────────────────────────────────────────────────────
langgraph~=0.2                 # API StateGraph estable desde 0.2

# ── OpenAI ────────────────────────────────────────────────────────────────────
openai~=1.40

# ── Vector store ──────────────────────────────────────────────────────────────
faiss-cpu~=1.8

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv~=1.0
pyyaml~=6.0                    # Requerido por spec/spec_loader.py (Mejora 1)

# ── Dev & quality ─────────────────────────────────────────────────────────────
black~=24.0
ruff~=0.4
pre-commit~=3.7
