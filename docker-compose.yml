services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: llull
      POSTGRES_USER: llull
      POSTGRES_PASSWORD: llull
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: